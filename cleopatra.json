{
    "config": {
        "type": "manual",
        "overrides": []
    },

    "deploy": [
        {
            "name": "Deployment Phase",
            "type": "local",
            "default": true,
            "clone": true,
            "steps": [
                {
                    "name": "Finalize",
                    "commands": [
                        "ln -sfn $DEPLOYMENT_PATH ../../current"
                    ]
                }
            ]
        }
    ]
}